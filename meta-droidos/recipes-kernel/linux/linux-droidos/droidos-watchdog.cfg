# Hardware watchdog (spec §5, §8). Started early in boot; the safety gateway pets
# it, and a stalled host causes the safety controller to remove motor power (§24).
CONFIG_WATCHDOG=y
CONFIG_WATCHDOG_SYSFS=y
CONFIG_WATCHDOG_HANDLE_BOOT_ENABLED=y
CONFIG_GPIO_WATCHDOG=m
